const config = {
	ENV: 'Local',
	Service: 'http://eycp.product.1396c.com', // 配置实际的接口地址，如果需要切换，请自行修改
	ServiceRouter: ['login', 'api', 'menu', 'user', 'lottery', 'transaction']
}

module.exports = config;
